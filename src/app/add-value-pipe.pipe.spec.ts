import { AddValuePipePipe } from './add-value-pipe.pipe';

describe('AddValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
